import './setting.scss';
import { BaseComponent } from '../../base-component';

export class Setting extends BaseComponent {
  constructor() {
    super('div', ['setting']);

    // this.element.innerHTML = `<p class="best-score__header">Best players</p>`;
    // this.element.appendChild(this.mainTimer.element);
  }
}
